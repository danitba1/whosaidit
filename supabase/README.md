# Supabase

1. Create a project at https://supabase.com
2. Run `migrations/001_init.sql` in the SQL editor
3. Enable Email auth for facilitators
4. Optionally run `seed.sql` after the first facilitator exists (fictional DEMO01 game)
5. Confirm Database → Publications includes `games` for Realtime
