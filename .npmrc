# Corporate TLS inspection re-signs HTTPS with a private root CA that Windows
# trusts but Node's bundled CA list does not. Without this, server-side fetches
# to Supabase fail with SELF_SIGNED_CERT_IN_CHAIN.
node-options=--use-system-ca
